为方便与ai交流项目内容，我想写一个 文件夹扁平化处理脚本
```
项目结构概览
- project-tools-node/
  - 文件夹扁平化处理脚本
- temp/

脚本在 project-tools-node/ 文件夹里写，是 node js 脚本
要符合此 jsconfig.json
{
  "compilerOptions": {
    "checkJs": true,
    "strict": true,
    "noImplicitAny": true,
    "strictNullChecks": true,
    "strictFunctionTypes": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true
  },
  "include": [
    "**/*.js"
  ]
}


接收参数：文件 或 文件夹路径，为绝对路径

脚本中可配置变量：
要处理的文件的类型，数组，如 ['.js', '.ts', '.vue']，即以此结尾的文件都会被处理

将文件夹遍历其中所有文件（及子文件中扽子文件夹及文件），将其整合为一个txt文件

【文件绝对路径】
空一行
文件文本内容
空两行
【文件绝对路径】
空一行
文件文本内容
空两行
……

整合后的.txt文件，文件名为这样的，将最开始脚本接收的参数（即路径）处理为文件名，比如将斜杠反斜杠冒号之类替换为下划线
在文件名之前，还要加上以当前时间处理后的字符串，如 20251207-181533-000_ 这样的前缀

再改进一些，我想让其可以同时传入多个路径，最终文件名还是以第一个为准
```